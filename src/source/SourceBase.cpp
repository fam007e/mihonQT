#include "SourceBase.h"

// The constructor and destructor are inline in the header,
// but Q_OBJECT requires a .cpp file to exist for MOC.
// No other implementation is needed for an abstract base class here.
